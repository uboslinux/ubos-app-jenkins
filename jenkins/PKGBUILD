developer=http://jenkins-ci.org/
url=${developer}
maintainer=http://indiecomputing.com/
pkgname=$(basename $(pwd))
pkgver=1.625.1
pkgrel=1
pkgdesc="An extendable open source continuous integration server"
arch=('any')
license=("MIT")
source=("jenkins_${pkgver}.war::http://mirrors.jenkins-ci.org/war-stable/${pkgver}/jenkins.war")
options=('!strip')
sha512sums=('b05710ffe195476fa22ca2dd226148755310ee17527c0294a0251414bbb085860fd40ec485ce53a4446d36bfaa908f3a4b828e1772d590433120ec3668c239d9')

package() {
# Manifest
    mkdir -p ${pkgdir}/var/lib/ubos/manifests
    install -m0644 ${startdir}/ubos-manifest.json ${pkgdir}/var/lib/ubos/manifests/${pkgname}.json

# Icons
    mkdir -p ${pkgdir}/srv/http/_appicons/$pkgname
    # install -m644 ${startdir}/appicons/{72x72,144x144}.png ${pkgdir}/srv/http/_appicons/$pkgname/

# Templates
    mkdir -p ${pkgdir}/usr/share/${pkgname}/tmpl
    install ${startdir}/tmpl/{context.xml.tmpl,htaccess.tmpl} ${pkgdir}/usr/share/${pkgname}/tmpl/

# Code
    mkdir -p ${pkgdir}/usr/share/${pkgname}/lib
    install ${startdir}/jenkins_${pkgver}.war ${pkgdir}/usr/share/${pkgname}/lib/jenkins.war
    mkdir -p ${pkgdir}/usr/share/${pkgname}/bin
    install ${startdir}/bin/move-plugins-dir.pl ${pkgdir}/usr/share/${pkgname}/bin
}
